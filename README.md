# Mucklo Boy's Day

8-bit side-scrolling platformer starring Mucklo Boy.

## Status

Recovered from git history of the `projects/site` hub repo and split into its own standalone project folder for migration to a dedicated repository.

## Provenance

Recovered from commit:
- `97d0692` — Add Mucklo Boy's Day — 8-bit side-scrolling platformer

Removed later in commit:
- `3dfba5b` — Clean mucklo repo — replace with full website, remove sub-projects

## Deploy

This project is intended to be deployed as a standalone static site.
